# cid-workflow-version: 0.5.0

# This file is generated by the CID Workflow GitLab App.
# DO NOT EDIT!

# global defaults
default:
  image: quay.io/podman/stable@sha256:642704dd0bcd909b722a06e0dbe199bc74163047886c3d5c869fe2c0d8e3d4d5 # v5.4.2-immutable
  before_script:
    - curl -L -o /usr/local/bin/cid https://github.com/cidverse/cid/releases/download/v0.5.0/linux_amd64
    - chmod +x /usr/local/bin/cid
    - cid version
    - cid catalog update
    - cid catalog list
    - cid executables update

# variables
variables:
  # git settings
  GIT_STRATEGY: clone
  GIT_DEPTH: 10

# stages
stages:
    - build
    - test
    - scan
    - publish

# configuration snippets
.default-job: &default_job
  tags:
    - saas-linux-small-amd64
  allow_failure: false
  interruptible: true
  retry:
    max: 2
    when:
      - runner_system_failure
      - api_failure
      - scheduler_failure

# Nightly
## gradle-build [test-library-kotlin-gradle]
"nightly/gradle-build [test-library-kotlin-gradle]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "gradle-build-test-library-kotlin-gradle"
    artifacts:
      name: "nightly/gradle-build [test-library-kotlin-gradle]"
      paths:
        - .dist/gradle-build-test-library-kotlin-gradle/
      expire_in: 1 day
## gradle-test [test-library-kotlin-gradle]
"nightly/gradle-test [test-library-kotlin-gradle]":
    <<: *default_job
    stage: test
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "gradle-test-test-library-kotlin-gradle"
    coverage: /Test-Coverage:(\d+\.\d+)%/ # used for percentage
    artifacts:
      name: "nightly/gradle-test [test-library-kotlin-gradle]"
      paths:
        - .dist/gradle-test-test-library-kotlin-gradle/
      expire_in: 1 day
      reports:
        junit: .dist/gradle-test-test-library-kotlin-gradle/report/junit/*.xml
        coverage_report: # used for visualization only
          coverage_format: jacoco
          path: .dist/gradle-test-test-library-kotlin-gradle/report/jacoco/*.xml
## gitleaks-scan
"nightly/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "gitleaks-scan"
    artifacts:
      name: "nightly/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"nightly/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "semgrep-scan"
    artifacts:
      name: "nightly/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"nightly/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "trivyfs-scan"
    artifacts:
      name: "nightly/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json

# Pull Request
## gradle-build [test-library-kotlin-gradle]
"pull-request/gradle-build [test-library-kotlin-gradle]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "gradle-build-test-library-kotlin-gradle"
    artifacts:
      name: "pull-request/gradle-build [test-library-kotlin-gradle]"
      paths:
        - .dist/gradle-build-test-library-kotlin-gradle/
      expire_in: 1 day
## gradle-test [test-library-kotlin-gradle]
"pull-request/gradle-test [test-library-kotlin-gradle]":
    <<: *default_job
    stage: test
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "gradle-test-test-library-kotlin-gradle"
    coverage: /Test-Coverage:(\d+\.\d+)%/ # used for percentage
    artifacts:
      name: "pull-request/gradle-test [test-library-kotlin-gradle]"
      paths:
        - .dist/gradle-test-test-library-kotlin-gradle/
      expire_in: 1 day
      reports:
        junit: .dist/gradle-test-test-library-kotlin-gradle/report/junit/*.xml
        coverage_report: # used for visualization only
          coverage_format: jacoco
          path: .dist/gradle-test-test-library-kotlin-gradle/report/jacoco/*.xml
## gitleaks-scan
"pull-request/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "gitleaks-scan"
    artifacts:
      name: "pull-request/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"pull-request/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "semgrep-scan"
    artifacts:
      name: "pull-request/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"pull-request/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "trivyfs-scan"
    artifacts:
      name: "pull-request/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json

# Release
## gradle-build [test-library-kotlin-gradle]
"release/gradle-build [test-library-kotlin-gradle]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "gradle-build-test-library-kotlin-gradle"
    artifacts:
      name: "release/gradle-build [test-library-kotlin-gradle]"
      paths:
        - .dist/gradle-build-test-library-kotlin-gradle/
      expire_in: 1 day
## gradle-test [test-library-kotlin-gradle]
"release/gradle-test [test-library-kotlin-gradle]":
    <<: *default_job
    stage: test
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "gradle-test-test-library-kotlin-gradle"
    coverage: /Test-Coverage:(\d+\.\d+)%/ # used for percentage
    artifacts:
      name: "release/gradle-test [test-library-kotlin-gradle]"
      paths:
        - .dist/gradle-test-test-library-kotlin-gradle/
      expire_in: 1 day
      reports:
        junit: .dist/gradle-test-test-library-kotlin-gradle/report/junit/*.xml
        coverage_report: # used for visualization only
          coverage_format: jacoco
          path: .dist/gradle-test-test-library-kotlin-gradle/report/jacoco/*.xml
## gitleaks-scan
"release/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "gitleaks-scan"
    artifacts:
      name: "release/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"release/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "semgrep-scan"
    artifacts:
      name: "release/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"release/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "trivyfs-scan"
    artifacts:
      name: "release/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json
## changelog-generate
"release/changelog-generate":
    <<: *default_job
    stage: publish
    needs: ["release/gitleaks-scan", "release/gradle-build [test-library-kotlin-gradle]", "release/gradle-test [test-library-kotlin-gradle]", "release/semgrep-scan", "release/trivyfs-scan"]
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "changelog-generate"
    artifacts:
      name: "release/changelog-generate"
      paths:
        - .dist/changelog-generate/
      expire_in: 1 day
## gitlab-release-publish
"release/gitlab-release-publish":
    <<: *default_job
    stage: publish
    needs: ["release/changelog-generate", "release/gitleaks-scan", "release/gradle-build [test-library-kotlin-gradle]", "release/gradle-test [test-library-kotlin-gradle]", "release/semgrep-scan", "release/trivyfs-scan"]
    dependencies: ["release/changelog-generate"]
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "gitlab-release-publish"
    artifacts:
      name: "release/gitlab-release-publish"
      paths:
        - .dist/gitlab-release-publish/
      expire_in: 1 day

# Main
## gradle-build [test-library-kotlin-gradle]
"main/gradle-build [test-library-kotlin-gradle]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "gradle-build-test-library-kotlin-gradle"
    artifacts:
      name: "main/gradle-build [test-library-kotlin-gradle]"
      paths:
        - .dist/gradle-build-test-library-kotlin-gradle/
      expire_in: 1 day
## gradle-test [test-library-kotlin-gradle]
"main/gradle-test [test-library-kotlin-gradle]":
    <<: *default_job
    stage: test
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "gradle-test-test-library-kotlin-gradle"
    coverage: /Test-Coverage:(\d+\.\d+)%/ # used for percentage
    artifacts:
      name: "main/gradle-test [test-library-kotlin-gradle]"
      paths:
        - .dist/gradle-test-test-library-kotlin-gradle/
      expire_in: 1 day
      reports:
        junit: .dist/gradle-test-test-library-kotlin-gradle/report/junit/*.xml
        coverage_report: # used for visualization only
          coverage_format: jacoco
          path: .dist/gradle-test-test-library-kotlin-gradle/report/jacoco/*.xml
## gitleaks-scan
"main/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "gitleaks-scan"
    artifacts:
      name: "main/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"main/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "semgrep-scan"
    artifacts:
      name: "main/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"main/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "trivyfs-scan"
    artifacts:
      name: "main/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json
